/* ====== CYBERPUNK ELEVATOR PORTFOLIO - ENHANCED ====== */

@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Space+Mono:wght@400;700&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    /* Updated color palette */
    --neon-cyan: #00f0ff;
    --neon-pink: #ff006e;
    --neon-purple: #7b2cbf;
    --neon-green: #39ff14;
    --dark-bg: #0a0e27;
    --dark-secondary: #1a1f3a;
    --text-primary: #e0e0e0;
    --text-secondary: #a0a0a0;

    /* Glass effect opacity */
    --glass-opacity: 0.1;
}

html,
body {
    width: 100%;
    height: 100%;
    overflow: hidden;
    background-color: var(--dark-bg);
    color: var(--text-primary);
    font-family: 'Space Mono', monospace;
    scroll-behavior: smooth;
}

/* ====== ACCESSIBILITY ====== */

@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}

.sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
}

.sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
}

/* ====== LOADING SCREEN ====== */

.loading-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    animation: fadeOutScreen 0.6s ease-out 2.4s forwards;
}

@keyframes fadeOutScreen {
    to {
        opacity: 0;
        pointer-events: none;
    }
}

.loading-content {
    text-align: center;
    max-width: 400px;
}

.terminal-text {
    font-family: 'Orbitron', monospace;
    margin-bottom: 2rem;
}

.loading-line {
    display: block;
    color: var(--neon-cyan);
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
    text-shadow: 0 0 10px var(--neon-cyan);
    animation: typeWriter 0.6s steps(40, end) forwards;
    letter-spacing: 2px;
}

@keyframes typeWriter {
    from {
        opacity: 0;
        width: 0;
    }

    to {
        opacity: 1;
        width: 100%;
    }
}

.loading-bar {
    width: 200px;
    height: 4px;
    background: rgba(0, 240, 255, 0.1);
    border: 2px solid var(--neon-cyan);
    margin: 0 auto;
    border-radius: 2px;
    overflow: hidden;
    box-shadow: 0 0 10px rgba(0, 240, 255, 0.3);
}

.loading-progress {
    height: 100%;
    background: linear-gradient(90deg, var(--neon-cyan), var(--neon-pink), var(--neon-purple));
    width: 0%;
    animation: progressBar 2.4s ease-out forwards;
    box-shadow: 0 0 10px var(--neon-cyan);
}

@keyframes progressBar {
    to {
        width: 100%;
    }
}

/* ====== SOUND TOGGLE ====== */

.sound-toggle {
    position: fixed;
    top: 18px;
    right: 75px;
    width: 42px;
    height: 42px;
    background: rgba(0, 240, 255, 0.1);
    border: 2px solid var(--neon-cyan);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 100;
    transition: all 0.3s ease;
    box-shadow: 0 0 10px rgba(0, 240, 255, 0.2);
    font-size: 1.2rem;
}

.sound-toggle:hover {
    background: rgba(0, 240, 255, 0.2);
    box-shadow: 0 0 20px rgba(0, 240, 255, 0.5);
    transform: scale(1.1);
}

.sound-toggle.muted {
    border-color: var(--neon-pink);
    box-shadow: 0 0 10px rgba(255, 0, 110, 0.2);
}

.sound-toggle.muted:hover {
    box-shadow: 0 0 20px rgba(255, 0, 110, 0.5);
}

.sound-icon {
    display: inline-block;
    transition: transform 0.3s ease;
}

.sound-toggle.muted .sound-icon {
    transform: rotate(45deg);
}

/* ====== TUTORIAL MODAL ====== */

.tutorial-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(10, 14, 39, 0.95);
    z-index: 10000;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(5px);
    animation: fadeInModal 0.4s ease-out;
}

.tutorial-modal.hidden {
    display: none;
}

@keyframes fadeInModal {
    from {
        opacity: 0;
    }

    to {
        opacity: 1;
    }
}

.tutorial-content {
    background: linear-gradient(135deg, rgba(0, 240, 255, 0.05), rgba(255, 0, 110, 0.05));
    border: 2px solid var(--neon-cyan);
    border-radius: 10px;
    padding: 2rem;
    max-width: 500px;
    max-height: 80vh;
    overflow-y: auto;
    box-shadow: 0 0 30px rgba(0, 240, 255, 0.3), inset 0 0 30px rgba(0, 240, 255, 0.1);
    position: relative;
}

.tutorial-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: none;
    border: none;
    color: var(--neon-cyan);
    font-size: 2rem;
    cursor: pointer;
    transition: all 0.2s ease;
    padding: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.tutorial-close:hover {
    color: var(--neon-pink);
    transform: scale(1.2);
}

.tutorial-content h2 {
    color: var(--neon-cyan);
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
    text-shadow: 0 0 10px var(--neon-cyan);
    letter-spacing: 2px;
}

.tutorial-sections {
    margin-bottom: 2rem;
}

.tutorial-section {
    margin-bottom: 1.5rem;
}

.tutorial-section h3 {
    color: var(--neon-green);
    font-size: 1.1rem;
    margin-bottom: 0.7rem;
    letter-spacing: 1px;
}

.tutorial-section p,
.tutorial-section li {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
}

.tutorial-section ul {
    margin-left: 1.5rem;
    margin-top: 0.5rem;
}

.tutorial-section li {
    margin-bottom: 0.5rem;
}

.tutorial-start {
    width: 100%;
    padding: 1rem;
    background: linear-gradient(135deg, var(--neon-cyan), var(--neon-purple));
    border: 2px solid var(--neon-cyan);
    color: var(--dark-bg);
    font-size: 1.1rem;
    font-weight: bold;
    cursor: pointer;
    border-radius: 5px;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 2px;
}

.tutorial-start:hover {
    box-shadow: 0 0 20px rgba(0, 240, 255, 0.6);
    transform: translateY(-2px);
}

/* ====== FLOOR LOCK INDICATOR ====== */

.floor-lock-indicator {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 60px;
    background: linear-gradient(90deg, rgba(0, 240, 255, 0.1), rgba(123, 44, 191, 0.1));
    border-bottom: 2px solid var(--neon-cyan);
    z-index: 150;
    display: none;
    align-items: center;
    justify-content: space-between;
    padding: 0 1.5rem;
    animation: slideDownIndicator 0.4s ease-out;
    box-shadow: 0 0 20px rgba(0, 240, 255, 0.2), inset 0 0 10px rgba(0, 240, 255, 0.1);
}

.floor-lock-indicator.visible {
    display: flex;
}

@keyframes slideDownIndicator {
    from {
        transform: translateY(-60px);
    }

    to {
        transform: translateY(0);
    }
}

.lock-content {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex: 1;
}



.unlock-btn {
    padding: 0.6rem 1.2rem;
    background: rgba(255, 0, 110, 0.1);
    border: 2px solid var(--neon-pink);
    color: var(--neon-pink);
    font-weight: bold;
    cursor: pointer;
    border-radius: 5px;
    transition: all 0.3s ease;
    font-size: 0.9rem;
    letter-spacing: 1px;
    text-transform: uppercase;
}

.unlock-btn:hover {
    background: var(--neon-pink);
    color: var(--dark-bg);
    box-shadow: 0 0 15px var(--neon-pink);
    transform: translateY(-2px);
}

/* ====== EXIT FLOOR BUTTON ====== */

.exit-floor-btn {
    position: fixed;
    bottom: 18px;
    right: 145px;
    padding: 0.65rem 0.9rem;
    background: linear-gradient(135deg, var(--neon-purple), var(--neon-pink));
    border: 2px solid var(--neon-pink);
    color: #fff;
    font-weight: bold;
    cursor: pointer;
    border-radius: 5px;
    z-index: 140;
    display: none;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 0.75rem;
    box-shadow: 0 0 15px rgba(255, 0, 110, 0.3);
    white-space: nowrap;
    min-height: 40px;
    min-width: 40px;
}

.exit-floor-btn.visible {
    display: flex;
    animation: slideInButton 0.4s ease-out;
}

@keyframes slideInButton {
    from {
        opacity: 0;
        transform: translateX(100px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}

.exit-floor-btn:hover {
    box-shadow: 0 0 25px rgba(255, 0, 110, 0.6), inset 0 0 10px rgba(255, 255, 255, 0.1);
    transform: translateY(-3px);
}

.exit-icon {
    display: inline-block;
    font-size: 1.1rem;
}

/* ====== BACKGROUND WRAPPER ====== */

.background-wrapper {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    overflow: hidden;
    background: #000;
}

.bg-layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity 1s ease-in-out;
    z-index: 1;
    overflow: hidden;
}

.bg-layer.active {
    opacity: 1;
    z-index: 2;
}

/* ====== ERA 1: PREHISTORIC (Dinosaur Age) ====== */
.bg-prehistoric {
    background: linear-gradient(180deg, #87CEEB 0%, #228B22 60%, #006400 100%);
}

.bg-prehistoric::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 50%;
    background:
        radial-gradient(circle at 20% 100%, #006400 0%, transparent 50%),
        radial-gradient(circle at 80% 100%, #228B22 0%, transparent 50%);
    opacity: 0.8;
}

/* Volcano */
.bg-prehistoric::after {
    content: '';
    position: absolute;
    bottom: 20%;
    left: 10%;
    width: 0;
    height: 0;
    border-left: 100px solid transparent;
    border-right: 100px solid transparent;
    border-bottom: 200px solid #3e2723;
}

/* ====== ERA 2: 1900s (Industrial) ====== */
.bg-1900s {
    background: #d7ccc8;
    /* Sepia-ish */
    filter: sepia(0.8) contrast(1.2);
}

.bg-1900s::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: repeating-linear-gradient(90deg,
            transparent,
            transparent 50px,
            rgba(0, 0, 0, 0.05) 50px,
            rgba(0, 0, 0, 0.05) 100px);
}

/* Smoke/Smog */
.bg-1900s::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 50% 50%, rgba(100, 100, 100, 0.2), transparent);
    animation: smogPulse 10s infinite alternate;
}

@keyframes smogPulse {
    from {
        opacity: 0.3;
        transform: scale(1);
    }

    to {
        opacity: 0.6;
        transform: scale(1.1);
    }
}

/* ====== ERA 3: PRESENT (Modern) ====== */
.bg-present {
    background: linear-gradient(180deg, #4facfe 0%, #00f2fe 100%);
}

.bg-present::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 30%;
    background: linear-gradient(0deg, rgba(255, 255, 255, 0.8) 0%, transparent 100%);
}

/* Clouds */
.bg-present::after {
    content: '☁️';
    position: absolute;
    top: 20%;
    left: 10%;
    font-size: 5rem;
    opacity: 0.8;
    animation: floatCloud 60s linear infinite;
    text-shadow: 100px 50px 0 #fff, 300px -20px 0 #fff;
    color: #fff;
}

@keyframes floatCloud {
    from {
        transform: translateX(-100px);
    }

    to {
        transform: translateX(100vw);
    }
}

/* ====== ERA 4: FUTURE (Cyberpunk) ====== */
.bg-future {
    background: #0a0e27;
}

/* Existing cyberpunk styles will apply to children of .bg-future */

/* ====== ERA 5: SPACE (Void) ====== */
.bg-space {
    background: radial-gradient(ellipse at bottom, #1b2735 0%, #090a0f 100%);
    overflow: hidden;
}

.bg-space::before {
    content: '';
    position: absolute;
    width: 2px;
    height: 2px;
    background: white;
    box-shadow:
        10vw 10vh #fff, 20vw 50vh #fff, 30vw 20vh #fff, 40vw 80vh #fff,
        50vw 10vh #fff, 60vw 90vh #fff, 70vw 40vh #fff, 80vw 20vh #fff,
        90vw 60vh #fff, 15vw 85vh #fff, 35vw 45vh #fff, 55vw 65vh #fff,
        75vw 15vh #fff, 95vw 35vh #fff;
    animation: twinkle 5s infinite;
}

@keyframes twinkle {

    0%,
    100% {
        opacity: 0.8;
    }

    50% {
        opacity: 0.4;
    }
}

.cityscape {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(180deg, #0d1b2a 0%, #0a0e27 50%, #0a0e27 100%);
    overflow: hidden;
}

/* ====== SKY LAYER ====== */

.sky-layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 40%;
    background: linear-gradient(180deg,
            rgba(13, 27, 42, 0.9) 0%,
            rgba(20, 40, 60, 0.7) 40%,
            rgba(30, 20, 50, 0.5) 100%);
    z-index: 1;
}

/* ====== BUILDINGS - FAR (SLOWEST PARALLAX) ====== */

.buildings-far {
    position: absolute;
    bottom: 60%;
    left: 0;
    width: 200%;
    height: 40%;
    z-index: 2;
    opacity: 0.5;
    animation: parallaxFar 40s linear infinite;
}

.building-svg {
    width: 100%;
    height: 100%;
    filter: drop-shadow(0 0 15px rgba(0, 240, 255, 0.1));
}

.window-far {
    animation: flicker 2.5s infinite;
}

.window-far:nth-child(odd) {
    animation-delay: 0.5s;
}

.window-far:nth-child(3n) {
    animation-delay: 1s;
}

@keyframes parallaxFar {
    0% {
        transform: translateX(0);
    }

    100% {
        transform: translateX(-50%);
    }
}

@keyframes flicker {

    0%,
    100% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.8;
    }

    75% {
        opacity: 0.3;
    }
}

/* ====== BUILDINGS - MID (MEDIUM PARALLAX) ====== */

.buildings-mid {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 200%;
    height: 60%;
    z-index: 3;
    animation: parallaxMid 60s linear infinite;
}

.building-mid,
.neon-accent {
    filter: drop-shadow(0 0 8px rgba(0, 240, 255, 0.15));
}

.neon-accent {
    animation: neonFlicker 3s infinite;
}

.neon-accent:nth-child(even) {
    animation: neonFlicker 3s infinite 0.5s;
}

@keyframes parallaxMid {
    0% {
        transform: translateX(0);
    }

    100% {
        transform: translateX(-50%);
    }
}

@keyframes neonFlicker {

    0%,
    100% {
        opacity: 0.8;
        filter: drop-shadow(0 0 8px currentColor);
    }

    50% {
        opacity: 1;
        filter: drop-shadow(0 0 20px currentColor);
    }
}

/* ====== FOG/CLOUD LAYERS ====== */

.fog-layer {
    position: absolute;
    width: 200%;
    height: 100%;
    background-repeat: repeat-x;
    opacity: 0.3;
    z-index: 5;
}

.fog-1 {
    bottom: 30%;
    background: linear-gradient(90deg,
            transparent 0%,
            rgba(0, 240, 255, 0.2) 25%,
            transparent 50%);
    background-size: 400px 100%;
    animation: fog-drift 30s linear infinite;
}

.fog-2 {
    bottom: 10%;
    background: linear-gradient(90deg,
            rgba(123, 44, 191, 0.1) 0%,
            transparent 50%);
    background-size: 500px 100%;
    animation: fog-drift 40s linear infinite reverse;
    opacity: 0.2;
}

@keyframes fog-drift {
    0% {
        transform: translateX(0);
    }

    100% {
        transform: translateX(-200px);
    }
}

/* ====== FLYING VEHICLES ====== */

.flying-vehicle {
    position: absolute;
    z-index: 4;
    filter: drop-shadow(0 0 10px rgba(255, 0, 110, 0.6));
}

#vehicle1 {
    top: 25%;
    right: -100px;
    animation: vehiclePass1 15s ease-in-out infinite;
}

#vehicle2 {
    top: 45%;
    right: -80px;
    animation: vehiclePass2 18s ease-in-out 3s infinite;
}

@keyframes vehiclePass1 {
    0% {
        right: -100px;
        opacity: 0;
    }

    10% {
        opacity: 1;
    }

    50% {
        opacity: 1;
    }

    90% {
        opacity: 0;
    }

    100% {
        right: 100vw;
        opacity: 0;
    }
}

@keyframes vehiclePass2 {
    0% {
        right: -80px;
        opacity: 0;
    }

    10% {
        opacity: 1;
    }

    50% {
        opacity: 0.8;
    }

    90% {
        opacity: 0;
    }

    100% {
        right: 100vw;
        opacity: 0;
    }
}

/* ====== NEON SIGNS ====== */

.neon-sign {
    position: absolute;
    font-family: 'Orbitron', sans-serif;
    font-size: 2rem;
    font-weight: bold;
    text-shadow: 0 0 20px currentColor, 0 0 40px currentColor;
    z-index: 5;
    animation: signBlink 3s infinite;
    letter-spacing: 4px;
}

.sign-1 {
    top: 20%;
    left: 10%;
    color: var(--neon-cyan);
    animation-delay: 0s;
}

.sign-2 {
    top: 50%;
    right: 15%;
    color: var(--neon-pink);
    font-size: 1.8rem;
    animation-delay: 0.8s;
}

.sign-3 {
    top: 35%;
    left: 50%;
    color: var(--neon-purple);
    font-size: 1.6rem;
    animation-delay: 1.5s;
}

@keyframes signBlink {

    0%,
    100% {
        opacity: 0.7;
        text-shadow: 0 0 20px currentColor, 0 0 40px currentColor;
    }

    50% {
        opacity: 1;
        text-shadow: 0 0 30px currentColor, 0 0 60px currentColor;
    }
}

/* ====== HEX GRID ====== */

.hex-grid {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 2;
    opacity: 0.1;
}

/* ====== DATA STREAMS ====== */

.data-streams {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 4;
    pointer-events: none;
    overflow: hidden;
}

.data-line {
    position: absolute;
    width: 2px;
    background: linear-gradient(180deg, var(--neon-green), transparent);
    opacity: 0.2;
    animation: dataFlow 4s linear infinite;
}

.line-1 {
    left: 15%;
    height: 40%;
    top: 10%;
    animation-delay: 0s;
}

.line-2 {
    left: 50%;
    height: 60%;
    top: 15%;
    animation-delay: 1s;
}

.line-3 {
    left: 75%;
    height: 50%;
    top: 20%;
    animation-delay: 2s;
}

@keyframes dataFlow {
    0% {
        transform: translateY(-100%);
        opacity: 0;
    }

    10% {
        opacity: 0.2;
    }

    90% {
        opacity: 0.2;
    }

    100% {
        transform: translateY(100vh);
        opacity: 0;
    }
}

/* ====== GLASS REFLECTION OVERLAY ====== */

.glass-reflection {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(ellipse at 50% 0%, rgba(0, 240, 255, 0.05), transparent 60%);
    z-index: 6;
    pointer-events: none;
    animation: glassShimmer 6s ease-in-out infinite;
}

@keyframes glassShimmer {

    0%,
    100% {
        background: radial-gradient(ellipse at 50% 0%, rgba(0, 240, 255, 0.05), transparent 60%);
    }

    50% {
        background: radial-gradient(ellipse at 50% 0%, rgba(255, 0, 110, 0.05), transparent 60%);
    }
}

/* ====== CRT FLICKER OVERLAY ====== */

/* CRT Flicker removed for modern look */

/* ====== GLOBAL SCANLINES ====== */

/* Global scanlines removed for modern look */

/* ====== ELEVATOR CONTAINER ====== */

.elevator-container {
    position: relative;
    width: 100%;
    text-decoration: none;
    transition: all 0.3s ease;
    text-shadow: 0 0 5px var(--neon-green);
    word-break: break-word;
}

.contact-item a:hover {
    color: var(--neon-pink);
    text-shadow: 0 0 15px var(--neon-pink);
}

.contact-message {
    font-size: 1rem;
    color: var(--neon-purple);
    margin-top: 1.5rem;
    text-shadow: 0 0 10px var(--neon-purple);
    animation: slideInUp 0.6s ease-out 0.2s both;
}

/* ====== ELEVATOR PANEL ====== */

.floating-controls {
    position: fixed;
    right: 18px;
    bottom: 22px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    align-items: center;
    z-index: 40;
}

.floating-controls .sound-toggle,
.floating-controls .exit-floor-btn,
.floating-controls .elevator-panel {
    pointer-events: auto;
}


.elevator-panel {
    width: 115px;
    background: linear-gradient(180deg, rgba(0, 240, 255, 0.08) 0%, rgba(255, 0, 110, 0.08) 100%);
    border-left: 3px solid var(--neon-cyan);
    border-right: 2px solid var(--neon-purple);
    padding: 1.5rem 0.8rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.2rem;
    box-shadow: inset 0 0 20px rgba(0, 240, 255, 0.1), inset -10px 0 20px rgba(0, 240, 255, 0.05);
    position: relative;
    z-index: 20;
}

.panel-header {
    font-family: 'Orbitron', sans-serif;
    color: var(--neon-cyan);
    font-size: 0.75rem;
    text-shadow: 0 0 5px var(--neon-cyan);
    letter-spacing: 2px;
    font-weight: bold;
    border-bottom: 2px solid var(--neon-cyan);
    padding-bottom: 0.5rem;
    width: 100%;
    text-align: center;
    box-shadow: 0 2px 10px rgba(0, 240, 255, 0.2);
}

.floor-buttons {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    flex: 1;
    width: 100%;
}

.floor-btn {
    position: relative;
    width: 100%;
    padding: 0.65rem;
    background: rgba(0, 240, 255, 0.08);
    border: 2px solid var(--neon-cyan);
    color: var(--neon-cyan);
    cursor: pointer;
    font-family: 'Orbitron', sans-serif;
    font-size: 0.65rem;
    transition: all 0.3s ease;
    box-shadow: 0 0 8px rgba(0, 240, 255, 0.3);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.2rem;
    overflow: hidden;
}

.floor-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 240, 255, 0.3), transparent);
    transition: left 0.5s ease;
}

.floor-btn:hover::before {
    left: 100%;
}

.floor-btn:hover {
    background: rgba(0, 240, 255, 0.18);
    box-shadow: 0 0 15px var(--neon-cyan), inset 0 0 10px rgba(0, 240, 255, 0.2);
    transform: scale(1.05);
}

.floor-btn.active {
    background: rgba(57, 255, 20, 0.2);
    border-color: var(--neon-green);
    color: var(--neon-green);
    box-shadow: 0 0 20px var(--neon-green), inset 0 0 15px rgba(57, 255, 20, 0.3);
    text-shadow: 0 0 10px var(--neon-green);
    animation: activeButtonPulse 2s infinite;
}

@keyframes activeButtonPulse {

    0%,
    100% {
        box-shadow: 0 0 20px var(--neon-green), inset 0 0 15px rgba(57, 255, 20, 0.3);
    }

    50% {
        box-shadow: 0 0 30px var(--neon-green), inset 0 0 20px rgba(57, 255, 20, 0.5);
    }
}

.floor-num {
    display: block;
    font-weight: bold;
    font-size: 0.75rem;
}

.floor-label {
    display: block;
    font-size: 0.55rem;
    opacity: 0.8;
}

.panel-indicator {
    background: rgba(0, 0, 0, 0.6);
    border: 2px solid var(--neon-purple);
    padding: 1rem;
    border-radius: 3px;
    text-align: center;
    box-shadow: 0 0 15px rgba(123, 44, 191, 0.3), inset 0 0 10px rgba(123, 44, 191, 0.1);
    margin-top: auto;
}

.indicator-text {
    font-size: 0.65rem;
    color: var(--neon-purple);
    text-shadow: 0 0 5px var(--neon-purple);
    margin-bottom: 0.5rem;
}

.current-floor {
    font-family: 'Orbitron', sans-serif;
    font-size: 1.8rem;
    color: var(--neon-pink);
    font-weight: bold;
    text-shadow: 0 0 15px var(--neon-pink);
    animation: floorCounter 0.4s ease-out;
}

@keyframes floorCounter {
    0% {
        opacity: 0;
        transform: scale(0.5) rotateZ(-20deg);
    }

    100% {
        opacity: 1;
        transform: scale(1) rotateZ(0deg);
    }
}

/* ====== RESPONSIVE DESIGN ====== */

@media (max-width: 1024px) {
    .elevator-panel {
        width: 105px;
        padding: 1.4rem 0.7rem;
    }

    .floor-btn {
        padding: 0.6rem;
        font-size: 0.6rem;
    }

    .exit-floor-btn {
        bottom: 16px;
        right: 130px;
        padding: 0.6rem 0.8rem;
        font-size: 0.7rem;
    }
}

@media (max-width: 768px) {
    .elevator-panel {
        width: 95px;
        padding: 1.3rem 0.5rem;
        gap: 0.9rem;
    }

    .sound-toggle {
        width: 40px;
        height: 40px;
        top: 15px;
        right: 110px;
        font-size: 1rem;
    }

    .exit-floor-btn {
        padding: 0.6rem 0.8rem;
        font-size: 0.7rem;
        bottom: 85px;
        right: 12px;
    }

    .floor-lock-indicator {
        padding: 0 1rem;
        height: 55px;
    }

    .floor-1 h1 {
        font-size: 1.7rem;
    }

    .skills-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
    }

    .timeline::before {
        left: 0;
    }

    .timeline-marker {
        left: 0;
    }

    .timeline-item {
        flex-direction: column;
        gap: 1rem;
    }

    .panel-indicator {
        padding: 0.6rem;
    }

    .current-floor {
        font-size: 1.3rem;
    }

    .neon-sign {
        font-size: 0.9rem;
        opacity: 0.6;
    }

    .buildings-far {
        opacity: 0.3;
    }

    .flying-vehicle {
        opacity: 0.6;
    }
}

@media (max-width: 480px) {
    .elevator-panel {
        width: 75px;
        padding: 0.9rem 0.35rem;
        gap: 0.7rem;
    }

    .sound-toggle {
        width: 38px;
        height: 38px;
        font-size: 1rem;
        top: 14px;
        right: 95px;
    }

    .exit-floor-btn {
        padding: 0.5rem 0.7rem;
        font-size: 0.65rem;
        bottom: 105px;
        right: 12px;
    }

    .floor-lock-indicator {
        height: 52px;
        padding: 0 0.9rem;
        font-size: 0.85rem;
    }

    .lock-icon {
        font-size: 1rem;
    }

    .lock-text {
        font-size: 0.9rem;
    }

    .unlock-btn {
        padding: 0.45rem 0.7rem;
        font-size: 0.7rem;
    }

    .floor-1 h1 {
        font-size: 1.4rem;
    }

    .floor-btn {
        padding: 0.5rem 0.25rem;
        font-size: 0.55rem;
    }

    .subtitle {
        font-size: 0.85rem;
    }

    .skills-grid,
    .projects-list,
    .contact-grid {
        gap: 0.8rem;
    }

    .skill-card {
        padding: 1.2rem;
    }

    .project-card {
        padding: 1.2rem;
    }

    .panel-header {
        font-size: 0.6rem;
    }

    .scroll-indicator {
        font-size: 0.75rem;
    }

    .glitch {
        font-size: 0.7rem;
    }
}

/* ====== REDUCED MOTION ====== */

@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }

    .crt-flicker {
        display: none;
    }
}